<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pust.parking.mapper.SysUserMapper">

    <resultMap id="SysUserMap" type="com.pust.parking.entity.SysUser">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="phone" property="phone"/>
        <result column="role" property="role"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="findByUsername" parameterType="string" resultMap="SysUserMap">
        SELECT id, username, password, phone, role, status, create_time, update_time
        FROM sys_user
        WHERE username = #{username}
    </select>

    <insert id="insert" parameterType="com.pust.parking.entity.SysUser" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user (username, password, phone, role, status)
        VALUES (#{username}, #{password}, #{phone}, #{role}, #{status})
    </insert>

</mapper>
