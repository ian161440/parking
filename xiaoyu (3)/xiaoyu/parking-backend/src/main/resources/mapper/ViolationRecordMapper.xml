<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pust.parking.mapper.ViolationRecordMapper">

    <resultMap id="ViolationRecordMap" type="com.pust.parking.entity.ViolationRecord">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="vehicle_id" property="vehicleId"/>
        <result column="space_id" property="spaceId"/>
        <result column="description" property="description"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="handler_id" property="handlerId"/>
    </resultMap>

    <select id="listAll" resultMap="ViolationRecordMap">
        SELECT id, user_id, vehicle_id, space_id, description, status, create_time, handler_id
        FROM violation_record
        ORDER BY create_time DESC
    </select>

    <select id="findById" parameterType="long" resultMap="ViolationRecordMap">
        SELECT id, user_id, vehicle_id, space_id, description, status, create_time, handler_id
        FROM violation_record
        WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.pust.parking.entity.ViolationRecord" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO violation_record (user_id, vehicle_id, space_id, description, status, handler_id)
        VALUES (#{userId}, #{vehicleId}, #{spaceId}, #{description}, #{status}, #{handlerId})
    </insert>

    <update id="updateStatus">
        UPDATE violation_record
        SET status = #{status},
            handler_id = #{handlerId}
        WHERE id = #{id}
    </update>

</mapper>
