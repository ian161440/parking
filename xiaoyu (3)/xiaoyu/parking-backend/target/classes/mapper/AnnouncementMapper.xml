<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pust.parking.mapper.AnnouncementMapper">

    <resultMap id="AnnouncementMap" type="com.pust.parking.entity.Announcement">
        <id column="id" property="id"/>
        <result column="title" property="title"/>
        <result column="content" property="content"/>
        <result column="is_top" property="isTop"/>
        <result column="publisher_id" property="publisherId"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="listAllOrderByTop" resultMap="AnnouncementMap">
        SELECT id, title, content, is_top, publisher_id, create_time, update_time
        FROM announcement
        ORDER BY is_top DESC, create_time DESC
    </select>

    <select id="findById" parameterType="long" resultMap="AnnouncementMap">
        SELECT id, title, content, is_top, publisher_id, create_time, update_time
        FROM announcement
        WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.pust.parking.entity.Announcement" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO announcement (title, content, is_top, publisher_id)
        VALUES (#{title}, #{content}, #{isTop}, #{publisherId})
    </insert>

    <update id="update" parameterType="com.pust.parking.entity.Announcement">
        UPDATE announcement
        SET title = #{title},
            content = #{content},
            is_top = #{isTop}
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="long">
        DELETE FROM announcement WHERE id = #{id}
    </delete>

</mapper>
